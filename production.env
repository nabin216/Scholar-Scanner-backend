# Production Environment Variables for Backend Deployment
# Copy these values to your deployment platform (Railway, Heroku, etc.)

# Django Core Settings
SECRET_KEY=$3y4%+=mi2*k20#rg^3t_ce^%pftzuq#eb(&+xa5jzk6h72(is
DEBUG=False
ALLOWED_HOSTS=*.railway.app,localhost,127.0.0.1

# Database (Railway provides PostgreSQL automatically)
# DATABASE_URL will be provided by Railway automatically

# AWS SES Configuration (Your existing credentials)
AWS_ACCESS_KEY_ID=AKIA2UCDHAJ7Y3XZHJHC
AWS_SECRET_ACCESS_KEY=BEl+kWSS5N/iYKvei3COboT5KwlmKly1G8OGIs7c//04
AWS_SES_REGION=eu-north-1
AWS_SES_FROM_EMAIL=support@scholarscanner.com

# CORS Settings (Update with your frontend domain after deployment)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.vercel.app
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Security Settings
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
X_FRAME_OPTIONS=DENY

# Rate Limiting (Production values)
THROTTLE_RATE_ANON=500/hour
THROTTLE_RATE_USER=5000/day
THROTTLE_RATE_AUTH=20/minute
THROTTLE_RATE_LOGIN=10/minute
THROTTLE_RATE_REGISTRATION=20/day
THROTTLE_RATE_EMAIL_VERIFICATION=30/hour

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440
